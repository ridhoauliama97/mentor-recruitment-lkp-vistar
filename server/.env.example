# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=rekrutmen_mentor_psi

# Server port (opsional, default 3001)
PORT=3001

# JWT secret key (opsional, fallback ke 'mentor-psi-secret-key')
JWT_SECRET=change-this-to-a-random-secret
